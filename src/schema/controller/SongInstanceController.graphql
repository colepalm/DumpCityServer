type Mutation {
    createSongInstance(
        id: String,
        setNumber: Int
        position: Int
        showId: String!,
        description: String
        jamChart: Boolean
    ): SongInstance
}
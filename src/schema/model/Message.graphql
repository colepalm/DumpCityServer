type Message {
    id: ID!
    text: String!
    createdAt: String!
    user: User!
}

type MessageConnection {
    edges: [Message!]!
    pageInfo: PageInfo!
}

type PageInfo {
    hasNextPage: Boolean!
    endCursor: String!
}